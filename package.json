{
  "name": "pi-shift-router",
  "version": "1.7.0",
  "description": "Per-turn model routing for Pi: a small judge picks the cheap or the strong tier for each message, with multi-model failover, task-level orchestration, and an optional decision-model judge (Jev) that answers with a calibrated probability instead of prose.",
  "keywords": [
    "pi-package",
    "pi-extension",
    "pi-coding-agent",
    "pi-agent",
    "shift-router",
    "model-routing",
    "ai-router",
    "llm-router",
    "orchestrator",
    "task-orchestration",
    "subagent",
    "model-selection",
    "task-classifier",
    "llm-judge",
    "two-tier-routing",
    "multi-model-fallback",
    "exponential-backoff",
    "agent-failover",
    "rate-limit-handling",
    "model-switching",
    "agent-routing",
    "auto-router",
    "cost-optimization",
    "jev",
    "typesafe",
    "decision-model",
    "calibrated-probability",
    "system-one",
    "judge-model",
    "model-tier-routing"
  ],
  "license": "MIT",
  "homepage": "https://shiftrouter.greenerai.top",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/green-dalii/pi-shift-router.git"
  },
  "bugs": {
    "url": "https://github.com/green-dalii/pi-shift-router/issues"
  },
  "author": {
    "name": "green-dalii",
    "url": "https://github.com/green-dalii"
  },
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "pi": {
    "extensions": [
      "./dist/index.js"
    ],
    "minPiVersion": "0.80.0",
    "image": "https://raw.githubusercontent.com/green-dalii/pi-shift-router/main/assets/hero.jpeg"
  },
  "engines": {
    "node": ">=24.0.0"
  },
  "publishConfig": {
    "registry": "https://registry.npmjs.org/"
  },
  "files": [
    "dist",
    "docs",
    "assets",
    "README.md",
    "README.zh-CN.md",
    "LICENSE",
    "CHANGELOG.md",
    "package.json"
  ],
  "scripts": {
    "build": "tsc && node scripts/copy-assets.mjs",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage",
    "clean": "rm -rf node_modules dist",
    "pack:check": "npm pack --dry-run && node scripts/pack-check.mjs",
    "prepublishOnly": "npm run test && npm run build && npm run pack:check",
    "check:isolated": "node scripts/check-isolated-load.mjs"
  },
  "peerDependencies": {
    "@earendil-works/pi-tui": "*"
  },
  "devDependencies": {
    "@earendil-works/pi-coding-agent": ">=0.80.0",
    "@earendil-works/pi-tui": "^0.81.1",
    "@types/node": "^22.0.0",
    "@vitest/coverage-v8": "^2.1.9",
    "typescript": "^5.8.0",
    "vitest": "^2.1.0"
  },
  "dependencies": {
    "@earendil-works/pi-tui": "*"
  }
}
